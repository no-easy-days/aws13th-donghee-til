# Part 1. 변수 실습

## 🔥 실습 1: 변수 선언 (기초)

자신의 이름, 나이, 좋아하는 숫자를 각각 변수에 저장하고 출력하는 코드를 작성하세요.


```
name, age, number = "고동희", 25, 7
print(name, age, number)
```

## 🔥 실습 2: 값 교환 (기초)

`first = "A"`와 `second = "B"` 두 변수의 값을 서로 교환하세요.

```
first, second = "A","B"
first, second = second, first
print(first, second)
```

## 🔥 실습 3: 복합 연산 (기초)

`balance = 10000`에서 시작하여 3000을 빼고, 2를 곱한 뒤 최종 값을 출력하세요.


```
balance = (10000-3000)*2
print(balance)
```

## 🔥 실습 4: 오류 수정 (응용)

아래 코드의 오류를 찾고 수정하세요

```python
2nd_place = "은메달"
user name = "홍길동"
class = "1학년"
```


```
place_2nd ="은메달"
user_name ="홍길동"
year = "1학년"
```

---

# Part 2. 자료형 실습

## 🔥 실습 5: 자료형 확인하기 (기초)

다음 값들의 자료형을 `type()` 함수로 확인하고 출력하세요

- `42`
- `3.14`
- `"Hello"`
- `True`
- `None`

```
print(type(42), type(3.14), type("Hello"), type(True), type(None))
```


---

## 🔥 실습 6: 형변환 연습 (기초)

사용자로부터 두 숫자를 입력받아 더한 값을 출력하는 프로그램을 작성하세요.

(힌트: `input()`은 문자열을 반환합니다)

```
a, b = map(int,input("두 숫자를 입력하시오: ").split())
print(a + b)
```

---

## 🔥 실습 7: 자기소개 프로그램 (응용)


사용자의 이름, 나이, 키를 입력받아 자기소개 문장을 출력하세요.

- 내년 나이도 함께 계산하여 출력
- f-string을 사용할 것
</aside>

```
user =(input("이름, 나이, 키를 입력하시오: ").split())
for i in range(1):
    print(f"이름:{user[0]} \n나이:{user[1]}\n키:{user[2]}\n내년 나이:{int(user[1])+1}") 
```
    

---

## 🔥 실습 8: 계산기 만들기 (심화)

두 숫자와 연산자를 입력받아 계산 결과를 출력하는 간단한 계산기를 만드세요.

- 덧셈, 뺄셈, 곱셈, 나눗셈, 몫, 나머지를 모두 지원
```
a,b = map(int,input("두 숫자를 입력하시오:").split())
Operator = input("연산자를 입력하시오: ")

if Operator == "+":
    print("덧셈:",(a + b))
elif Operator == "-":
    print("뺄셈:",a - b)
elif Operator == "*":
    print("곱셈:",a * b)
elif Operator == "/":
    print("나눗셈:",a / b)
elif Operator == "//":
    print("몫:",a // b)
elif Operator == "%":
    print("나머지:",a % b)
else:
    print("지원하지 않는 연산자입니다.")
    
```










