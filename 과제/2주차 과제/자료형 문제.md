# 3.1. 자료형 실습 문제

## 🔥 실습 3-1: 이메일 주소 분리하기

사용자로부터 이메일 주소를 입력받아 사용자 이름과 도메인을 분리하여 출력하세요.

**입력 예시**: [`jeff@example.com`](mailto:jeff@example.com)

**출력 예시**:

```
사용자 이름: jeff
도메인: [example.com](http://example.com)
```

```
user_email = input("이메일 주소를 입력하시오:").split("@")
print(f"사용자 이름:{user_email[0]} \n 도메인:{user_email[1]}")
```

## 🔥 실습 3-2: 비밀번호 길이 검사

비밀번호가 8자 이상인지 확인하는 프로그램을 작성하세요.

**조건**: 길이가 8자 이상이면 "✅ 유효한 비밀번호입니다." 출력
```
password = input("비밀번호 8자 이상 입력하시오:").strip()
if len(password) >= 8:
    print("✅ 유효한 비밀번호입니다.")
else:
    print("❌ 유효하지 않는 비밀번호입니다.")
```


## 🔥 실습 3-3: 3의 배수 찾기

1부터 20까지의 숫자 중에서 3의 배수만 리스트에 담아 출력하세요.

**출력 예시**: `[3, 6, 9, 12, 15, 18]`

```
result = []
for number in range(1,21):
    if number % 3 == 0:
        result.append(number)

print(result)
```

## 🔥 실습 3-4: 공통 관심사 찾기


철수와 영희의 관심사 리스트가 주어졌을 때, 공통 관심사를 찾아보세요.

```
chulsoo = ["축구", "영화", "음악", "게임", "독서"]
younghee = ["영화", "음악", "요리", "여행", "독서"]

set_ch = set(chulsoo)
set_yo = set(younghee)

common = set_yo & set_ch
print(common)
```

## 🔥 실습 3-5: 최고 점수 학생 찾기

학생들의 점수가 딕셔너리로 주어졌을 때, 최고 점수를 받은 학생을 찾아보세요.

```python
scores = {
    "철수": 85,
    "영희": 92,
    "민수": 78,
    "지수": 95,
    "현우": 88
}
```
```
max_v = ""
max_n = 0

for v, n in scores.items():
    if n > max_n:
        max_v = v
        max_n = n
print(max_v)
```
